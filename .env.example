OPENAI_API_KEY=
# Create once locally via `npm run openai:sync` and reuse in Vercel.
OPENAI_VECTOR_STORE_ID=

# Optional model overrides
OPENAI_ANSWER_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
